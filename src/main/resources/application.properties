## IMPORTANT: do NOT commit secrets into git. Use environment variables or an external
## configuration source in production. This file reads values from environment variables.
spring.data.mongodb.uri=${MONGODB_URI}
spring.data.mongodb.database=${MONGODB_DATABASE:chatdb}
server.port=${PORT:8080}
jwt.secret=${JWT_SECRET:defaultSecret123}

# spring.security.oauth2.client.registration.google.client-id=${GOOGLE_CLIENT_ID:}
# spring.security.oauth2.client.registration.google.client-secret=${GOOGLE_CLIENT_SECRET:}
# spring.security.oauth2.client.registration.google.scope=profile,email
